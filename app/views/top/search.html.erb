


<div class="container">
              <ul class="listview js-lazy-load-images">
                <% if @lessons.present?  then @lessons.each do |lesson| %>
                

                    <div class="row no-space-bottom">
                      <div class="col6 push6">
                        <h3 class="text-middle text-break">
                          <span class="color-sub"><%= lesson.title %></span>
                        </h3>
                        <h4><%= lesson.teacher %></h4><a href="/review/<%= lesson.id %>">レビューを書く</a>
                        <a href="/reviews/<%= lesson.id %>">レビューを見る</a>
                        <h5><%= lesson.context %></h5>
                        <p class="text-xsmall text-overflow">

                          <!-- 説明 -->
                        </p>
                      </div>
                    </div>
<!--                   </div> -->
                
                <%end%>
                <div>
                  <%= paginate(@lessons) %>
                </div>
                <%end%>

</div>